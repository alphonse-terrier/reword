.DS_Store
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
# Package.resolved (kept for reproducible builds — the same dependency versions everywhere,
# including CI) lives under project.xcworkspace/xcshareddata/swiftpm/, so don't blanket-ignore it.
.swiftpm/
xcuserdata/
.build/
*.dmg
